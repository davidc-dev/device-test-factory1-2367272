apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: my-app-data
  namespace: device-apps
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: lvms-vg1
