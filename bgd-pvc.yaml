apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{DEVICE_NAME}}-{{DEVICE_ID}}-pvc
  namespace: device-apps
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: lvms-vg1
